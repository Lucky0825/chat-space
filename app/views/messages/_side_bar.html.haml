.side
  .side__top
    .side__top__box
      = current_user.name
      .side-top-box-box
        =link_to "#", class: "side_top_box-box_btn" do
          = icon('fas', 'edit')
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog', class: 'icon')
  .side-content
    .groups
      =link_to "#", class: "groups" do
        .group
          family
        .chat
          こんにちは
    .groups
      =link_to "#", class: "groups" do
        .group
          school
        .chat
          おはよう
    .groups
      =link_to "#", class: "groups" do
        .group
          office
        .chat
          お疲れ様